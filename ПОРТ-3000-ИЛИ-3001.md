# 🔧 Работа с портами 3000 и 3001

## 📋 Текущая ситуация:

Сервер запустился на порту **3001** вместо **3000**, потому что порт 3000 был занят.

---

## ✅ Решение 1: Использовать порт 3001 (быстро)

Если порт 3000 все еще занят, можно использовать порт 3001:

### В браузере откройте:
```
http://test.localhost:3001
```

**НО!** Это не будет работать, потому что в базе данных creator настроен на домен `test.localhost:3000`.

---

## ✅ Решение 2: Освободить порт 3000 (правильно)

### Шаг 1: Остановите ВСЕ процессы Next.js

В терминале выполните:
```bash
# Остановите текущий сервер
Ctrl + C

# Убейте все процессы на портах 3000 и 3001
lsof -ti:3000 | xargs kill -9 2>/dev/null
lsof -ti:3001 | xargs kill -9 2>/dev/null

# Или убейте все процессы next dev
pkill -f "next dev"
```

### Шаг 2: Подождите 2-3 секунды

### Шаг 3: Запустите сервер снова

```bash
npm run dev
```

### Шаг 4: Проверьте порт

Вы должны увидеть:
```
▲ Next.js 14.x.x
- Local:        http://localhost:3000
✓ Ready
```

**НЕ** должно быть сообщения "Port 3000 is in use".

---

## 🎯 После успешного запуска на порту 3000:

Используйте правильные адреса:
- ✅ **http://test.localhost:3000** (главная страница)
- ✅ **http://test.localhost:3000/login** (вход)
- ✅ **http://test.localhost:3000/subscribe** (подписка)

---

## 💡 Если порт 3000 все еще занят:

### Проверьте, что занимает порт:

```bash
lsof -i:3000
```

### Остановите процесс вручную:

Найдите PID процесса и убейте его:
```bash
kill -9 <PID>
```

Или убейте все процессы Node.js:
```bash
pkill -9 node
```

**Внимание:** Это остановит ВСЕ процессы Node.js на вашем компьютере!

---

## 🚀 Рекомендуемый порядок действий:

1. **Остановите сервер**: `Ctrl + C` в терминале
2. **Освободите порты**: 
   ```bash
   lsof -ti:3000 | xargs kill -9 2>/dev/null
   lsof -ti:3001 | xargs kill -9 2>/dev/null
   ```
3. **Подождите 2-3 секунды**
4. **Запустите сервер**: `npm run dev`
5. **Проверьте**: Должно быть `http://localhost:3000`
6. **Откройте в браузере**: `http://test.localhost:3000`

---

## ✅ Готово!

После выполнения этих шагов сервер должен работать на порту 3000.


